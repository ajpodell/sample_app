<% provide(:title, 'All users') %>
<h1>All users</h1>

<%= will_paginate %>

<ul class="users">
    <!-- Old method -->
    <% @users.each do |user| %>
        <li>
            <%= gravatar_for user, size: 52 %>
            <%= link_to user.name, user %>
        </li>
    <% end %>
    
    <!-- After rails partial refactoring -->
   <!--  <%= render @users %> -->

</ul>

<%= will_paginate %>